{
  "name": "junie-explorer",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "analyze": "node --loader ts-node/esm --no-warnings src/task-analysis.ts",
    "analyze:html": "node --loader ts-node/esm --no-warnings src/task-analysis.ts --format html:docs/index.html"
  },
  "bin": {
    "junie-explorer": "dist/task-analysis.js"
  },
  "description": "A tool for analyzing Junie task data with support for multiple output formats",
  "usage": "junie-explorer [--format|-f <format1[:filepath],format2[:filepath],...>]",
  "formats": {
    "console": "Default format that outputs tables to the console or to a file if filepath is specified",
    "json": "Outputs data in JSON format to stdout or to a file if filepath is specified",
    "html": "Outputs data as a single self-contained HTML file to stdout or to a file if filepath is specified"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  },
  "private": true
}
